-- SELECT 
--     FLOOR(PRICE / 10000) * 10000 PRICE_GROUP,
--     COUNT(PRODUCT_ID) PRODUCTS
-- FROM PRODUCT
-- GROUP BY FLOOR(PRICE / 10000) * 10000
-- ORDER BY FLOOR(PRICE / 10000) * 10000 ASC;

SELECT
    PRICE_GROUP, COUNT(PRICE_GROUP) PRODUCTS
FROM (
    SELECT FLOOR(PRICE / 10000) * 10000 PRICE_GROUP
    FROM PRODUCT
)
GROUP BY PRICE_GROUP
ORDER BY PRICE_GROUP;
